<Window x:Class="WpfExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModel="clr-namespace:WpfExplorer.ViewModel"
        xmlns:local="clr-namespace:WpfExplorer" xmlns:cmd="http://www.galasoft.ch/mvvmlight" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    
    <Window.DataContext>
        <ViewModel:MainWindowViewModel x:Name="mvm"></ViewModel:MainWindowViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Name="TB_Ping" Margin="10,10,129,376">Connecting to Database...</TextBlock>
        <TextBlock Grid.Column="0" Grid.Row="0" Name="TB_PingTime" Margin="150,10,6,376"></TextBlock>
        <TextBlock FontWeight="Bold" Grid.Column="1" Grid.Row="0" Name="TB_SearchText" Margin="0,8,1,396" Grid.ColumnSpan="2">Suchen Sie nach einem Dateinamen oder nach dem Inhalt einer Datei</TextBlock>
        <TextBox HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="0" Name="tb_Search" TextChanged="tb_Search_TextChanged" Height="25" Margin="0,25,6,367"></TextBox>
        <Button Name="DetectUSB" Height="30" Click="Detect_Click" Margin="0,361,6,28" Grid.Column="1" Grid.Row="0" Foreground="White">Neue Datenträger erkennen</Button>
        <Button HorizontalAlignment="Stretch" Name="IndexNewFiles" Height="30" Click="Index_Click" Margin="0,60,6,329" Grid.Column="1" Grid.Row="0" Foreground="White">Neue Dateien indizieren</Button>
        <Label HorizontalAlignment="Stretch" FontWeight="Bold" Content="Dateien ausschließen"  Margin="0,95,6,291" Grid.Column="1" Grid.Row="0"></Label>
        <TextBox Name="tb_AddExceptions" DataContext="" Height="25" Margin="0,120,6,273" Grid.Column="1">
            <!--
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="KeyDown">
                    <i:InvokeCommandAction Command="{Binding KeyInputCommand}" CommandParameter="YouCommandParameter"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            -->
        </TextBox>
        <ListBox Margin="0,155,10,67" Height="200" Name="ListBox" Grid.Column="1" BorderThickness="1px" BorderBrush="Black">

        </ListBox>
        <Grid Name="GD_Dateiausgabe" Margin="10,60,10,68" Grid.Column="0">

        </Grid>
        <TextBlock Name="IndexProgress" Margin="10,396,10,10" Grid.ColumnSpan="2"></TextBlock>

    </Grid>
</Window>
